
共同
--------
* 所有物品图片均可看大图
		done
* 图片等上传文件的保存地址可以配置
		defaults.put("fileupload.folder", "uploads");
		defaults.put("fileupload.temp", "tmp");
		defaults.put("fileupload.sizeMax", "1024");

* 重要节点，如发布订单、取消订单、确认收货等，需要用户再次确认

新建订单
--------
* “说明”可以正常填写显示 
    done


> 品牌可填、可选
> 在选择类别时，“返回”按钮是返回上一级菜单

购物车
--------
* 可以正确按国家分组
* 可以正常按国家隐藏订单
* checkbox功能可用，不同代购国家之间排他，需要给出提示
* 发布旁的信息（总计，等）数值正确

发布订单
--------
* 默认地址自动填入

订单一览（买家）
--------
* 可以正确按国家分组
* 可以正常按国家隐藏订单

订单详细（买家）
--------
* 订单个字段显示正确
* 未开始的阶段不可点击进入
* 所有阶段买家均可放弃，但在支付定金后，放弃需要付给买手定金的30%

订单详细（买手）
--------
* 收货阶段，显示买家的详细地址
* 需要追加“快递公司”字段
* 如果是“人肉背回”，则不需要填写快递单号
* 所有阶段买手均可放弃，暂时无惩罚

订单详细（买手）
--------
* 头像可以上传、修改